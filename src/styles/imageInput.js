import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
  imageLabelStyle: {
    width: 0,
    visibility: "hidden",
  },
  photoIcon: {
    fontSize: "20px",
    cursor: "pointer",
  },
});
